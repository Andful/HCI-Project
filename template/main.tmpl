<!DOCTYPE html>
<html>
<head>
<title>User Interface</title>
{{template "style"}}
</head>
<body>
	<button id="start" type="button" onclick="setTimeout(function() { sleeping.updateSize();schedule.updateSize(); }, 200);">Click Me!</button>
	<div id="app">
		{{template "tabs"}}
		{{template "script"}}
	</div>
</body>
</html>